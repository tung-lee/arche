# Onboarding Guide

> Stub — populate once dev environment is stable.

## Prerequisites
<!-- Node version, required tools -->

## Setup Steps
1. Clone the repo
2. Install dependencies: `npm install`
3. Copy `.env.example` to `.env.local` and fill in values
4. Run the dev server

## Project Structure
<!-- Quick tour of the codebase for new contributors -->

## First Tasks
<!-- Suggested starter tasks -->
