# Feature: [Feature Name]

> **Status:** `draft` | `in-progress` | `complete` | `deferred`
> **Phase:** v1 / v1.1 / v2
> **Last updated:** [date]

---

## Summary

One paragraph: what this feature does and why it exists.

---

## Users

Who uses this feature, and when in their journey?

---

## User Stories

- As a **[user type]**, I want to **[do something]** so that **[outcome/benefit]**
- As a **[user type]**, I want to **[do something]** so that **[outcome/benefit]**

---

## Behaviour

### Happy Path
Step-by-step description of how this feature works in the normal case.

1. User does X
2. System does Y
3. User sees Z

### Edge Cases & Rules
- Rule or constraint 1
- Rule or constraint 2
- What happens when something goes wrong

---

## Connections

Which other features or systems does this touch?

- **Depends on:** [feature/system]
- **Triggers:** [feature/system]
- **Shares data with:** [feature/system]

---

## MVP vs Full Version

| Aspect | MVP (v1) | Full Version |
|--------|----------|--------------|
| | | |

---

---

## Security Considerations

> Populated by feature-planner based on context/developer/SECURITY.md.
> Any item listed here is a build requirement, not optional.

<!-- Examples:
- Input validation: [which fields, what rules]
- Auth required: [which user types, what checks]
- Sensitive data: [what must not be stored, logged, or exposed]
- Rate limiting: [which endpoints, what limits]
- None identified
-->

## Tasks

> Granular implementation steps for this feature.
> Each task has a global T-number that matches TASK-LIST.md.
> Keep status here in sync with the central task list.
>
> Status: [ ] todo  [~] in progress  [x] done  [-] blocked  [>] deferred

| Task # | Status | What needs to be done |
|--------|--------|-----------------------|
| T—     | `[ ]`  | —                     |

<!-- When populating: break into the smallest independently-buildable units.
     Each row = something that can be built, tested, and verified on its own.
     Assign T-numbers from TASK-LIST.md in sequence. -->

---

---

## User Acceptance Tests

> Plain-English browser tests generated after this feature is built.
> The full interactive checklist lives in [feature-name]-uat.md once generated.
>
> UAT status: `pending` | `in-progress` | `passed` | `failed` | `partial`

**UAT Status:** `pending`

**Last tested:** —

**Outcome:** —

<!-- After UAT, record a brief summary here:
     "Passed — all 4 steps working as expected"
     "Failed — Step 2 (wrong password) showed a blank screen instead of error message"
     "Partial — Steps 1-3 passed, Step 4 (email confirmation) not yet set up" -->

## Open Questions

- [ ] Question that needs answering before or during build

---

## Notes

Any additional context, links, or decisions specific to this feature.

---

## Archive

<!-- Outdated content goes here — never delete, just move down -->
